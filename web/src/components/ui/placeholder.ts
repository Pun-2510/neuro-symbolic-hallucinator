/**
 * shadcn/ui components — sẽ được thêm khi chạy `npx shadcn@latest add button card ...`.
 *
 * Hiện tại: thiết kế dùng Tailwind classes trực tiếp + Radix primitives
 * (Dialog, Progress) đã có trong package.json. Khi cần Button/Card/Table thật
 * theo shadcn spec, chạy:
 *
 *     npx shadcn@latest add button card table dialog
 *
 * TODO(user): tuần 13 — sinh shadcn components đầy đủ.
 */

export {};