# =====================================================================
# Essay Integrity Checker — environment variables
# Copy sang `.env` và điền giá trị thật. KHÔNG commit `.env` lên git.
# =====================================================================

# --- App ---
APP_ENV=development
APP_DEBUG=true
APP_LOG_LEVEL=INFO

# --- Database ---
DATABASE_URL=sqlite:///./data/app.db

# --- API paths (override defaults trong config.yaml) ---
# CROSSREF_BASE_URL=https://api.crossref.org
# OPENALEX_BASE_URL=https://api.openalex.org
# SEMANTIC_SCHOLAR_BASE_URL=https://api.semanticscholar.org/graph/v1
# ARXIV_BASE_URL=http://export.arxiv.org/api

# --- Polite pool email (gửi kèm User-Agent đến Crossref/OpenAlex) ---
CONTACT_EMAIL=student@tdtu.edu.vn

# --- Semantic Scholar API key (optional, tăng rate limit) ---
# Lấy miễn phí tại https://www.semanticscholar.org/product/api
S2_API_KEY=

# --- Thresholds (override defaults trong config.yaml) ---
# ABSTENTION_LOW=0.4
# ABSTENTION_HIGH=0.6
# TITLE_SIM_VERIFIED=0.9
# TITLE_SIM_METADATA_ERROR=0.6

# --- Paths ---
DATA_DIR=./data
CACHE_DIR=./data/cache
CONFIG_FILE=./configs/config.yaml
